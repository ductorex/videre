# Trigger file for the dynamic mirror collector in this directory's conftest.py.
# `pytest_collect_file` turns this single file into one virtual module per
# `tests/videre_tests/test_*.py`, re-run against the shaped text renderer.
# No tests are defined here.
